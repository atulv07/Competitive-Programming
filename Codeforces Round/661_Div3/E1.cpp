#include<bits/stdc++.h>
using namespace std;

struct node {
	int w;
	node left, right;
}

int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	#endif

	int t;
	long long int n, s;
	cin >> t;
	while(t--) {
		cin >> n >> s;
		for(int i = 0; i < n - 1; i++) {
			cin >> a >> b >> w;
		}
	}
	return 0;
}